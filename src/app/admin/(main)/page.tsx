import { ScreenPlaceholder } from "@/components/ui";

export default function AdminDashboardPage() {
  return (
    <ScreenPlaceholder
      title="ダッシュボード"
      sub="システム全体の概況。アカウント数・コース数・進捗サマリーを表示します。"
      note="ダッシュボードは Phase D で実装します。"
    />
  );
}
