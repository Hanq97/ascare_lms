# ===== Database (khớp docker-compose.yml) =====
DATABASE_URL="postgresql://ascare:ascare_dev_pw@localhost:5432/ascare_lms?schema=public"

# ===== Auth (Auth.js) =====
AUTH_SECRET="thay-bang-chuoi-ngau-nhien-dai"   # tạo: openssl rand -base64 32

# ===== Demo seed password =====
SEED_PASSWORD="Care@2026"

# ===== App URL (link trong mail) =====
APP_URL="http://localhost:3000"

# ===== Mail (dev: Mailpit) — bỏ SMTP_HOST để dùng driver console =====
SMTP_HOST="localhost"
SMTP_PORT="1025"
MAIL_FROM="ASCare LMS <no-reply@ascare.local>"

# ===== AWS S3 + CloudFront (Phase 6) =====
AWS_REGION="ap-northeast-1"
AWS_S3_BUCKET="ascare-lms-videos"
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
CLOUDFRONT_DOMAIN=""
CLOUDFRONT_KEY_PAIR_ID=""
CLOUDFRONT_PRIVATE_KEY=""
